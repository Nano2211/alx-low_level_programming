#include "main.h"
/**
 * _islower- Checks if a char is lowercase, 
 * returns 1 if true and 0 if false
 *
 * Return: Always 0
 */
int _islower (int c)
{
	if (c = 97 && c <= 122)
	{
		return (1);
	}
		else
		{
			return (0);
		}
	_putchar('\n');
}
